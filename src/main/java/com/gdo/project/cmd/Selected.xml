<?xml version='1.0' encoding='iso-8859-1' ?>

<!DOCTYPE template PUBLIC
  "-//StudioGdo//DTD Stencil 1.0//EN" "stencil.dtd">

<template java="com.gdo.project.cmd.Selected">
	<description>
		Abstract class, just used as a common class for all
		selected command.
	</description>

	<!-- model part -->
	<slot name="Source" arity="*">
		<description>slot path where the selected stencils can be found (for
			selection)</description>
		<description>should be redefined (usually delegated slot)</description>
	</slot>

	<!-- selected stencils -->
	<slot name="Selected" arity="*">
		<description>slot where the selected stencils are plugged</description>
	</slot>

	<!-- used in rendering the command -->
	<prop name="StencilType" type="string">
		<data>stencil</data>
	</prop>

</template>
